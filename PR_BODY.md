# Duplicate files cleanup

This PR adds a script to detect duplicate files by SHA-256 hash and an initial empty report. Run the script locally to produce the full report and optionally delete duplicates by committing the removals.
