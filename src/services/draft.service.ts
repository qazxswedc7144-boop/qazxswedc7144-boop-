
/**
 * DEPRECATED - Draft System Removed
 * All transactions are now immediate and final (Post-Only).
 */
export const draftService = {
  saveDraft: async () => {},
  getDraft: async () => null,
  clearDraft: () => {}
};
